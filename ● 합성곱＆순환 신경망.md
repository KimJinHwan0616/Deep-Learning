## ??? ???
```angular2html
# ?? ??
model = keras.Sequential( name='?? ??' )    
# ??? ? 
model.add(
    keras.layers.Conv2D(
        ?? ??
        , kernel_size = ?? ??
        , activation='relu'     # ??? ??(??)
#       , padding = 'valid'     # ??(???)
        , padding = 'same'      # ??(??)
        , strides = 1           # ?????
        , input_shape = (??, ??, ??)    # ?? ??? ??(??? ?)
        )
)
# ??? (??)
model.add(keras.layers.MaxPooling2D( ?? ?? ))
# ??? (??)
# keras.layers.AveragePooling2D( ?? ?? ))
```


## ?? ???
```angular2html
# ?? ??
model = keras.Sequential( name='?? ??' )   
# ??? ? 
model.add(keras.layers.Embedding(
    ?? ?? ??
    , ?? ?? ??
    , input_length = ?? ?? ??    # pad_sequences
    )
)
# ??? 
model.add(
    keras.layers.SimpleRNN(
    ?? ??
    , activation='tanh'    # ??? ??(????? ???)
#   , dropout = ???? ??
    , return_sequences = False    # ???? ?? ??(??: True)
    )
)
# ??? 
model.add(
    keras.layers.Dense(
    ?? ??
    , activation = 'softmax/sigmoid'    # ??? ??(?????/?????)
    )
)
```

